# terraform-exercise

Possible improvements:
* terraform plan, apply only after approval (while reusing tfplan file)
* remote state and lock
* add ssh key to ec2 via terraform
* add test with assertion that instance is running and required file exists on disk
* add output with the instance IP